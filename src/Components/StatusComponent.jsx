import { Chip, Typography } from "@mui/material";

const StatusComponent = () => {
  return (
    <>
      <Chip label="In progress" size="small" sx={{ fontSize: "0.6rem" }} />
    </>
  );
};

export default StatusComponent;
